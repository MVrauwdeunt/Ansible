packages: 
  - htop
  - samba
  - vim

update : sudo apt update && sudo apt upgrade -y
install : sudo apt install 

git_url: 'https://github.com/MVrauwdeunt/Ansible'
vault_password_file: vault.yml

git_repos:
  - { repo: 'Ansible', url: 'https://github.com/MVrauwdeunt/Ansible' }

